@model IEnumerable<BaseModel.SubGrupo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model._Grupo.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Inativo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item._Grupo.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Inativo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubGrupoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubGrupoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubGrupoID })
        </td>
    </tr>
}

</table>
