@model BaseModel.Grupo

@{
    ViewBag.Title = "Detalhes do Grupo";
}

<h2>@ViewBag.Title</h2>

<div>
    <h4>Grupo</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nome)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Nome)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Editar", "EditGrupo", new { id = Model.GrupoID }) |
    @Html.ActionLink("Voltar para a lista de Grupos", "Index")
</p>
